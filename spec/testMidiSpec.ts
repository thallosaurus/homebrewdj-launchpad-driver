import "jasmine";
//import { TrackControlStripKeys, controlStripKeyToNumber } from '../src/widgets/TrackControlStrip';

describe("TrackControlStrip", () => {
    it("should return correct key number", () => {
//        expect(controlStripKeyToNumber(5)).toBe(0);
    });
});